# Talk to API directly (avoids dev-proxy issues). Requires CORS on the server (already enabled).
VITE_API_URL=http://localhost:4001

# Dev only: skip login UI and use mock session. Requires server AUTH_BYPASS=true for API calls.
VITE_BYPASS_AUTH=true
